<div class="menu">
	<% if current_user %>
		Welcome <%= current_user.name %> | <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
	<% else %>
		<%= link_to 'Register', new_user_registration_path %>
		<%= link_to 'Sign In', new_user_session_path %>
	<% end %>
</div>
